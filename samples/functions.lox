fun add(a, b) {
  a + b
}

println(add(5, 3));

fun makeCounter() {
  var count = 0;
  return fn(step) {
    count = count + step;
    count
  };
}

var counter = makeCounter();
println(counter(1));
println(counter(1));
println(counter(5));
